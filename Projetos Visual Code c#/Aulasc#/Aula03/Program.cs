using System;

class ExemplosVariaveisAula03
{
    static void Main()
    {
        // Exemplo de variável do tipo int (inteiro)
        int idade = 30;

        // Exemplo de variável do tipo double (número de ponto flutuante)
        double salario = 2500.75;

        // Exemplo de variável do tipo string (texto)
        string nome = "João";

        // Exemplo de variável do tipo bool (booleano)
        bool estaChovendo = true;

        // Exemplo de variável do tipo char (caractere)
        char genero = 'M';

        // Exemplo de variável do tipo decimal (número decimal de alta precisão)
        decimal saldo = 1050.25M;

        // Exemplo de variável do tipo DateTime (data e hora)
        DateTime dataNascimento = new DateTime(1995, 7, 15);

        // Exemplo de variável do tipo float (número de ponto flutuante de precisão simples)
        float altura = 1.75f;

        // Exemplo de variável do tipo long (inteiro longo)
        long populacaoMundial = 8000000000;

        // Exemplo de variável do tipo byte (byte)
        byte codigoAscii = 65;

        // Exemplo de variável do tipo short (inteiro curto)
        short temperatura = -10;

        // Exemplo de variável do tipo uint (inteiro sem sinal)
        uint contador = 1000;

        // Exemplo de variável do tipo ushort (inteiro curto sem sinal)
        ushort comprimento = 255;

        string Nome = "Mateus";
        var n = Nome;
        var Temp = temperatura;

        Console.WriteLine("O nome da variavel é " +n);

    }
}
